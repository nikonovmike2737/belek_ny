# Отступ якорной навигации

Начало каждого целевого раздела должно оставаться полностью видимым под закрепленной шапкой. Для этого используются `scroll-padding-top` на странице и `scroll-margin-top` на целевых разделах со значением не меньше высоты шапки с небольшим запасом.
